generated: '2026-08-17'
method: searched
source: https://uh.live/llms.txt
note: >-
  Provenance for the two llms.txt files in this directory, because they are not
  interchangeable and it matters which one an agent reads.
files:
  - file: allo-media-llms-provider.txt
    method: searched
    source: https://uh.live/llms.txt
    http_status: 200
    content_type: text/plain
    bytes: 1945
    fetched: '2026-08-17'
    verbatim: true
    generator: Yoast SEO v28.2
    assessment: >-
      This IS the provider's own llms.txt, saved verbatim — but it is a WordPress
      SEO plugin artifact, not a developer resource. Yoast generated it from the
      marketing site: five localised homepages, five blog articles, five blog
      categories, five tags, and a sitemap link. It names none of the company's
      six API surfaces, no base URL, no authentication, no SDK, and does not
      mention docs.allo-media.net at all. An agent that reads only this file
      cannot discover that Allo-Media has an API.
      Recorded as the honest finding it is: llms.txt adoption here is an SEO
      side-effect rather than an agent-readiness decision.
  - file: allo-media-llms.txt
    method: generated
    source: apis.yml + this repo's artifacts
    verbatim: false
    assessment: >-
      Generated by API Evangelist from the catalog profile. Carries what a
      developer or agent actually needs: the four API entries with base URLs and
      operations, the OAuth client_credentials flow and both token TTLs, the
      pagination/filter/exclude conventions, the documented rate limits, the four
      documented error statuses, the two official SDKs with versions and publish
      dates, and the explicit statement that no OpenAPI/MCP/agent-card exists.
      This is the file the `LLMsTxt` pointer in apis.yml targets.
x-note: >-
  Both are kept deliberately. Deleting the provider's file would hide a real
  measurement (they serve one, and it is empty of API content); replacing ours
  with theirs would leave the catalog with no usable machine summary of this
  provider.
